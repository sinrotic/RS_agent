# Project Instructions

## 中文输出要求

本项目中的说明文档、过程日志、复盘材料、报告草稿默认使用中文输出。只有在代码标识符、命令、配置项、英文论文/模型名或用户明确要求英文时，才保留英文。

## 面试导向的工程过程日志

在本项目中处理非平凡任务时，需要维护面试导向的工程过程日志。日志目标不是暴露私有思维链，而是沉淀可复述、可验证、适合面试表达的工程叙事。

对于有实际技术含量的任务，最终回复和必要的文档记录应尽量覆盖：

- 遇到的问题：任务中出现了什么障碍、歧义、缺陷或工程取舍。
- 定位方式：如何诊断问题，尽量引用具体文件、命令、测试、指标或输出证据。
- 解决方式：做了什么改动，为什么选择这个方案。
- 验证结果：用什么测试、命令、结果文件或指标证明方案有效。
- 面试可讲点：如何把这段工作解释成项目经历中的技术亮点。

要求：

- 记录要简洁、事实化、证据导向。
- 不要记录逐步的私有思考过程，不要写成流水账。
- 不要为简单的一行修改或纯机械操作过度记录。
- 如果任务涉及性能、效果、数据质量或模型策略，优先记录“问题 → 诊断 → 取舍 → 方案 → 验证 → 可扩展方向”。

## 工程叙事日志文档

只有当用户明确要求记录，或任务明显影响项目架构、模型策略、评估结果、数据处理链路、工程取舍或面试叙事时，才在验证完成后，将简短条目追加到：

`dic/ENGINEERING_NARRATIVE_LOG.md`

普通小修、小查询、简单重命名、一次性命令不需要更新该文档。条目默认使用中文，格式建议包含：

- 日期
- 任务
- 遇到的问题
- 定位方式
- 解决方式
- 验证结果
- 面试可讲点

如果已有更具体的优化记录文档，例如 `dic/OPTIMIZATION_NARRATIVE.md`，可以根据任务类型选择更新更合适的文档，但仍需保持中文、简洁、证据导向。
