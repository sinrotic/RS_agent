# Project Instructions

## 中文输出要求

本项目中的说明文档、过程日志、复盘材料、报告草稿默认使用中文输出。只有在代码标识符、命令、配置项、英文论文/模型名或用户明确要求英文时，才保留英文。

## 本地运行资源限制

本项目允许本机运行命令，但需预估内存占用：默认按 **12GB 可承受、14GB 为上限** 控制；预计超过 14GB 的测试、样本生成、评估、训练或大数据/模型加载任务应改为限流 smoke 或远程运行。

